[Unit]
Description=Game Service OneDaysTalk (Actix-web)
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/app/app

# docker-compose.yml の環境変数を指定
Environment="SERVER_HOST=127.0.0.1"
Environment="SERVER_PORT=8006"
Environment="DATABASE_URL=sqlite:ondyst.db"
Environment="RUST_LOG=info,ondyst=debug,actix_web::middleware=warn"

# 実行ファイル
ExecStart=/app/app/ondyst.exe

# クラッシュ時の自動再起動設定
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
